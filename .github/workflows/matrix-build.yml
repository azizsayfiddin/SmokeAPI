name: Matrix Build

on:
  workflow_call:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [ Windows ]
        bitness: [ 64 ]

    uses: acidicoala/KoalaBox/.github/workflows/build-cmake.yml@master
    with:
      module: SmokeAPI
      os: ${{ matrix.os }}
      bitness: ${{ matrix.bitness }}
